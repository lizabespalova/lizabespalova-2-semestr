#pragma once
#include <iostream>
#include <windows.h>
#include <iomanip>
#define SOLVE 
#define DEBUG
using namespace std;
int Array(int* arr, int size);
int Negative_numbers(int* arr, int size);
int Replacement(int* arr, int size);