#pragma once
#include <iostream>
#include <windows.h>
#include <iomanip>
#include <cstdlib>
#include <ctime>
using namespace std;
void b(int* arr, int size);
void c(int* arr, int size);
void d(int* arr, int size);
int B (int* arr, int size, int);
int C (int* arr, int size, int);
int D (int* arr, int size, int);
