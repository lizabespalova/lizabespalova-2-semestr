#pragma once
#include <iostream>
#include <windows.h>
#include <iomanip>
#include <cstdlib>
#include <ctime>
#define DEBUG
using namespace std;
int bcount(int* A, int size);
int crazm(int* A, int size);
int drazmer(int*A, int size);
void B(int* A, int* b, int size, int count, bool vivod);
void C(int* A, int* c, int size, int count, bool vivod);
void D(int* A, int* d, int size, int count, bool vivod);

